



    
        /* SHIFT ALT A -> atalho pra comentário de bloco 
        tudo que fizermos aqui é porgramação JavaScript*/

        // CTR ; -> Atalho pra comentário de uma linha 

        // Comando de saída
        console.log("Olá JavaScript!");
        /* Variáveis e constantes
        var -> Variável de escopo global
        let -> variável de escopo global ou de bloco
        const -> constante de escopo global ou de bloco (precisa ser inicializada com valor) */

        // Exemplos
        // variável
        let ano = 2003;
        //Constante   
        const nome = "Kaique";  


        console.log(nome);
        console.log(ano);
        console.log(nome, ano);
        console.log(ano, nome);
        
        

        /* O aluno Kaique está estudando JavaScript no ano de 2022. */
        /* CONCATENAÇÃO (tradicional) */
        console.log( " O aluno "+nome+" está estudando JavaScript no ano de "+ano);

        /* TEMPLATE LITERAL/STRING (moderno) */
            console.log(`O aluno ${nome} está está estudando JavaScript no ano de ${ano}.`); 